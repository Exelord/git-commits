{
  "name": "git-commits",
  "displayName": "Git Commits",
  "description": "Adds list of last commits in source control tab",
  "version": "1.3.1",
  "publisher": "Exelord",
  "repository": "https://github.com/Exelord/git-commits",
  "license": "MIT",
  "icon": "icon.png",
  "bugs": {
    "url": "https://github.com/Exelord/git-commits/issues"
  },
  "homepage": "https://github.com/Exelord/git-commits#readme",
  "engines": {
    "vscode": "^1.32.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "git",
    "commits",
    "list",
    "atom",
    "vscode"
  ],
  "activationEvents": [
    "onView:gitCommits"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "views": {
      "scm": [
        {
          "id": "gitCommits",
          "name": "Commits",
          "when": "gitOpenRepositoryCount != 0"
        }
      ]
    },
    "commands": [
      {
        "command": "git.undoCommit",
        "title": "Undo last commit",
        "icon": "$(discard)"
      },
      {
        "command": "gitCommits.copyCommitHash",
        "title": "Copy commit hash",
        "icon": "$(files)"
      },
      {
        "command": "gitCommits.copyFilePath",
        "title": "Copy file path",
        "icon": "$(files)"
      },
      {
        "command": "gitCommits.revertFile",
        "title": "Revert changes",
        "icon": "$(discard)"
      },
      {
        "command": "gitCommits.openFile",
        "title": "Open file",
        "icon": "$(go-to-file)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "git.undoCommit",
          "when": "view == gitCommits",
          "group": "navigation"
        }
      ],
      "view/item/context": [
        {
          "command": "gitCommits.copyCommitHash",
          "when": "view == gitCommits && viewItem == commitNode",
          "group": "inline"
        },
        {
          "command": "gitCommits.openFile",
          "when": "view == gitCommits && viewItem == fileNode",
          "group": "inline@1"
        },
        {
          "command": "gitCommits.revertFile",
          "when": "view == gitCommits && viewItem == fileNode",
          "group": "inline@2"
        },
        {
          "command": "gitCommits.copyFilePath",
          "when": "view == gitCommits && viewItem == fileNode",
          "group": "inline@3"
        }
      ]
    }
  },
  "extensionDependencies": [
    "vscode.git"
  ],
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install",
    "test": "npm run compile && node ./node_modules/vscode/bin/test",
    "release": "release-it"
  },
  "devDependencies": {
    "@types/mocha": "^7.0.1",
    "@types/node": "^13.7.6",
    "release-it": "^12.6.2",
    "tslint": "^6.0.0",
    "typescript": "^3.8.2",
    "vsce": "^1.74.0",
    "vscode": "^1.1.36"
  },
  "release-it": {
    "git": {
      "tagName": "v${version}",
      "commitMessage": "v${version}"
    },
    "npm": {
      "publish": false
    },
    "github": {
      "release": true
    },
    "hooks": {
      "after:release": "vsce publish"
    }
  },
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
